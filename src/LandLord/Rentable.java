package LandLord;
public interface Rentable {
     // Any class that implements has access to the methods in the interface this is why we don't need to use the keyword implements
      Double APPRECIATION_FEES = 30.00;
      Double firstPayment();
}
